---
phase: 09-backtesting-engine
plan: 04
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - src/backtesting/performance/metrics.py
  - src/backtesting/performance/reporting.py
  - src/backtesting/performance/tearsheet.py
  - src/backtesting/cli/backtest_runner.py
autonomous: true
requirements: [BACK-04]

must_haves:
  truths:
    - "Performance metrics match real trading characteristics"
    - "Comprehensive tearsheet provides detailed strategy analysis"
    - "CLI interface enables easy backtesting execution"
    - "Reports integrate Monte Carlo uncertainty quantification"
  artifacts:
    - path: "src/backtesting/performance/metrics.py"
      provides: "Trading-specific performance calculations"
      exports: ["PerformanceCalculator"]
      min_lines: 150
    - path: "src/backtesting/performance/reporting.py"
      provides: "Report generation and formatting"
      exports: ["ReportGenerator"]
      min_lines: 120
    - path: "src/backtesting/performance/tearsheet.py"
      provides: "Professional tearsheet generation"
      exports: ["TearsheetGenerator"]
      min_lines: 100
    - path: "src/backtesting/cli/backtest_runner.py"
      provides: "Command-line interface for backtesting"
      exports: ["BacktestCLI"]
      min_lines: 80
  key_links:
    - from: "src/backtesting/performance/metrics.py"
      to: "portfolio equity curve"
      via: "performance calculation from time series"
      pattern: "equity_curve.*sharpe.*drawdown.*win_rate"
    - from: "src/backtesting/performance/tearsheet.py"
      to: "Monte Carlo results"
      via: "uncertainty visualization"
      pattern: "confidence_intervals.*bootstrap.*simulation"
    - from: "src/backtesting/cli/backtest_runner.py"
      to: "complete backtesting pipeline"
      via: "orchestration of all components"
      pattern: "BacktestEngine.*WalkForward.*MonteCarlo"
---

<objective>
Build comprehensive performance reporting system and CLI interface for complete backtesting solution

Purpose: Provide professional-grade performance analysis and easy-to-use interface for running complete backtesting workflows
Output: Performance metrics, tearsheet generation, and CLI runner that orchestrates the entire backtesting system
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-backtesting-engine/09-RESEARCH.md
@.planning/phases/09-backtesting-engine/09-01-SUMMARY.md
@.planning/phases/09-backtesting-engine/09-02-SUMMARY.md
@.planning/phases/09-backtesting-engine/09-03-SUMMARY.md
</context>

<interfaces>
<!-- Key interfaces from all prior backtesting plans -->

From Plan 09-01:
```python
# Core backtesting components
class BacktestEngine:
    def run_backtest(self, start_date: str, end_date: str) -> dict

class Portfolio:
    def get_equity_curve(self) -> pd.DataFrame
    def get_trade_history(self) -> pd.DataFrame
    def get_performance_metrics(self) -> dict
```

From Plan 09-02:
```python
# Optimization components
class WalkForwardOptimizer:
    def optimize(self, data: pd.DataFrame, param_grid: dict) -> pd.DataFrame

class StrategyIntegrator:
    def generate_signals(self, market_data: dict) -> List[Signal]
```

From Plan 09-03:
```python
# Monte Carlo components
class MonteCarloSimulator:
    def simulate_backtests(self, strategy_config: dict) -> pd.DataFrame

class StatisticalAnalyzer:
    def calculate_confidence_intervals(self, results: pd.DataFrame) -> dict
    def statistical_significance_test(self, results: pd.DataFrame) -> dict
```

Standard Libraries:
```python
# Visualization and reporting
import matplotlib.pyplot as plt
import pandas as pd
import numpy as np
import argparse
from typing import Dict, List, Optional
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create comprehensive performance metrics calculator</name>
  <files>src/backtesting/performance/__init__.py, src/backtesting/performance/metrics.py</files>
  <behavior>
    - PerformanceCalculator computes trading-specific metrics from equity curve
    - Includes Sharpe ratio, Sortino ratio, max drawdown, win rate, profit factor
    - Handles annualization and risk-free rate adjustments
    - Supports both daily and trade-based analysis
  </behavior>
  <action>
    Create PerformanceCalculator class with comprehensive trading metrics. Implement calculate_all_metrics method that takes equity curve and trade history. Include standard metrics: total_return, annualized_return, sharpe_ratio, sortino_ratio, max_drawdown, volatility, win_rate, profit_factor, calmar_ratio. Add _calculate_sharpe, _calculate_max_drawdown, _calculate_win_rate helper methods. Use 252 trading days for annualization. Handle edge cases (no trades, all losses/gains). Follow research formulas for accurate financial calculations including risk-free rate handling.
  </action>
  <verify>
    <automated>python -m pytest tests/backtesting/test_performance.py::test_metrics_calculation -x</automated>
  </verify>
  <done>Performance calculator computes comprehensive trading metrics with proper annualization</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tearsheet generator with Monte Carlo integration</name>
  <files>src/backtesting/performance/tearsheet.py, src/backtesting/performance/reporting.py</files>
  <behavior>
    - TearsheetGenerator creates professional performance reports
    - Integrates Monte Carlo confidence intervals in visualizations
    - Generates equity curve plots with uncertainty bands
    - Includes key statistics table with confidence bounds
  </behavior>
  <action>
    Implement TearsheetGenerator class for professional reporting. Create generate_tearsheet method that produces comprehensive report with equity curve chart, drawdown plot, monthly returns heatmap, and statistics table. Integrate Monte Carlo results to show confidence intervals as shaded regions around equity curve. Create ReportGenerator class for HTML/PDF output formatting. Include _plot_equity_curve, _plot_drawdown, _create_stats_table helper methods using matplotlib. Add uncertainty visualization from Monte Carlo confidence intervals around performance metrics.
  </action>
  <verify>
    <automated>python -m pytest tests/backtesting/test_tearsheet.py::test_tearsheet_generation -x</automated>
  </verify>
  <done>Tearsheet generator creates professional reports with Monte Carlo uncertainty visualization</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create CLI interface for complete backtesting workflow</name>
  <files>src/backtesting/cli/__init__.py, src/backtesting/cli/backtest_runner.py</files>
  <behavior>
    - BacktestCLI orchestrates complete backtesting pipeline
    - Supports basic backtest, walk-forward optimization, and Monte Carlo analysis
    - Configurable parameters via command line arguments
    - Outputs results to files and displays key metrics
  </behavior>
  <action>
    Create BacktestCLI class as main entry point for backtesting. Implement run_backtest, run_walk_forward, and run_monte_carlo methods that orchestrate respective components. Add command-line argument parsing with argparse for: start_date, end_date, strategy_config, n_simulations, output_dir. Create main() function that coordinates workflow: data loading → strategy integration → backtesting → Monte Carlo → reporting. Include progress reporting and result file generation (CSV, HTML, plots). Add configuration validation and error handling for robust CLI experience.
  </action>
  <verify>
    <automated>python -m pytest tests/backtesting/test_cli.py::test_cli_workflow -x</automated>
  </verify>
  <done>CLI interface orchestrates complete backtesting workflow with configuration and output management</done>
</task>

</tasks>

<verification>
Complete performance reporting system verification:
- Performance metrics calculator provides trading-specific analytics
- Tearsheet generator creates professional reports with uncertainty visualization
- CLI interface enables easy execution of complete backtesting workflows
- Integration of all backtesting components functional
</verification>

<success_criteria>
- Comprehensive performance metrics calculation operational
- Professional tearsheet generation with Monte Carlo uncertainty visualization
- CLI interface provides complete backtesting workflow orchestration
- All backtesting components integrated into cohesive system
- Ready for integration with main OptionsAI API in Phase 10
</success_criteria>

<output>
After completion, create `.planning/phases/09-backtesting-engine/09-04-SUMMARY.md`
</output>