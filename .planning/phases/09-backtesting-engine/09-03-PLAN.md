---
phase: 09-backtesting-engine
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - src/backtesting/monte_carlo/simulator.py
  - src/backtesting/monte_carlo/bootstrap.py
  - src/backtesting/monte_carlo/analysis.py
autonomous: true
requirements: [BACK-03]

must_haves:
  truths:
    - "Monte Carlo simulation shows statistical robustness of results"
    - "Bootstrap resampling tests sequence dependency risk"
    - "Confidence intervals quantify result uncertainty"
    - "Trade sequence randomization validates strategy stability"
  artifacts:
    - path: "src/backtesting/monte_carlo/simulator.py"
      provides: "Monte Carlo simulation engine"
      exports: ["MonteCarloSimulator"]
      min_lines: 120
    - path: "src/backtesting/monte_carlo/bootstrap.py"
      provides: "Bootstrap resampling for trade sequences"
      exports: ["BootstrapResampler"]
      min_lines: 80
    - path: "src/backtesting/monte_carlo/analysis.py"
      provides: "Statistical analysis and confidence intervals"
      exports: ["StatisticalAnalyzer"]
      min_lines: 100
  key_links:
    - from: "src/backtesting/monte_carlo/simulator.py"
      to: "trade sequence randomization"
      via: "bootstrap resampling"
      pattern: "np\\.random\\.choice.*trade_returns.*replace=True"
    - from: "src/backtesting/monte_carlo/bootstrap.py"
      to: "portfolio equity curves"
      via: "resampled performance calculation"
      pattern: "cumulative.*drawdown.*final_return"
    - from: "src/backtesting/monte_carlo/analysis.py"
      to: "statistical significance"
      via: "confidence interval calculation"
      pattern: "scipy\\.stats.*percentile.*confidence"
---

<objective>
Build Monte Carlo simulation system for statistical validation of backtesting results

Purpose: Quantify uncertainty in backtesting results and validate strategy robustness against trade sequence risk through bootstrap resampling
Output: Monte Carlo simulation framework with bootstrap resampling and statistical analysis
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-backtesting-engine/09-RESEARCH.md
@.planning/phases/09-backtesting-engine/09-01-SUMMARY.md
@.planning/phases/09-backtesting-engine/09-02-SUMMARY.md
</context>

<interfaces>
<!-- Key interfaces from prior plans -->

From Plan 09-02:
```python
# Walk-forward optimization results
class WalkForwardOptimizer:
    def optimize(self, data: pd.DataFrame, param_grid: dict) -> pd.DataFrame
    # Returns DataFrame with period_start, period_end, params, return, sharpe, max_dd

# Strategy integration results
class StrategyIntegrator:
    def generate_signals(self, market_data: dict) -> List[Signal]
```

From Plan 09-01:
```python
# Portfolio performance data
class Portfolio:
    def get_equity_curve(self) -> pd.DataFrame
    def get_trade_history(self) -> pd.DataFrame
    def get_performance_metrics(self) -> dict
```

Standard Libraries:
```python
# Statistical analysis tools
import numpy as np
import pandas as pd
from scipy import stats
from typing import List, Dict, Tuple, Optional
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Monte Carlo simulation engine</name>
  <files>src/backtesting/monte_carlo/__init__.py, src/backtesting/monte_carlo/simulator.py</files>
  <behavior>
    - MonteCarloSimulator runs multiple resampled backtests
    - Configurable number of simulations (default 1000)
    - Random seed control for reproducible results
    - Collects distribution of performance metrics across simulations
  </behavior>
  <action>
    Implement MonteCarloSimulator class following research pattern. Include __init__ with n_simulations (default 1000), random_seed (default 42). Create simulate_backtests method that runs multiple backtests with resampled parameters or trade sequences. Implement _setup_simulation helper to prepare each simulation run. Collect results including final returns, Sharpe ratios, max drawdowns, and other key metrics. Use numpy random seed for reproducibility and scipy.stats for statistical functions. Return structured results as DataFrame for analysis.
  </action>
  <verify>
    <automated>python -m pytest tests/backtesting/test_monte_carlo.py::test_simulation_engine -x</automated>
  </verify>
  <done>Monte Carlo simulation engine runs multiple resampled backtests with statistical collection</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create bootstrap resampling for trade sequences</name>
  <files>src/backtesting/monte_carlo/bootstrap.py</files>
  <behavior>
    - Bootstrap resamples trade returns to test sequence dependency
    - Calculates performance distribution from resampled sequences
    - Preserves trade count but randomizes order
    - Generates cumulative returns and drawdown statistics
  </behavior>
  <action>
    Create BootstrapResampler class implementing trade sequence resampling from research. Implement resample_trade_sequence method that takes list of trade returns and randomly resamples with replacement using np.random.choice. Create _calculate_performance_metrics helper that computes cumulative returns, max drawdown, and final return from resampled sequence. Add simulate_trade_sequences method following research pattern that runs multiple bootstrap iterations. Use numpy.random for sampling and include proper statistical handling for edge cases (empty trades, all losses/gains).
  </action>
  <verify>
    <automated>python -m pytest tests/backtesting/test_bootstrap.py::test_trade_resampling -x</automated>
  </verify>
  <done>Bootstrap resampler tests trade sequence dependency with statistical robustness</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create statistical analysis and confidence intervals</name>
  <files>src/backtesting/monte_carlo/analysis.py</files>
  <behavior>
    - StatisticalAnalyzer calculates confidence intervals for performance metrics
    - Generates percentile-based confidence bounds (90%, 95%, 99%)
    - Tests statistical significance of results vs benchmarks
    - Provides risk assessment with tail statistics
  </behavior>
  <action>
    Implement StatisticalAnalyzer class for Monte Carlo results analysis. Create calculate_confidence_intervals method using scipy.stats.percentile for 90%, 95%, 99% confidence bounds. Implement statistical_significance_test for comparing strategy results against benchmarks using appropriate statistical tests (t-test, Wilcoxon). Add risk_analysis method that calculates VaR (Value at Risk), CVaR (Conditional VaR), and tail ratios from simulation distributions. Include _format_results helper for clean statistical reporting with confidence intervals and significance levels.
  </action>
  <verify>
    <automated>python -m pytest tests/backtesting/test_statistical_analysis.py::test_confidence_intervals -x</automated>
  </verify>
  <done>Statistical analyzer provides confidence intervals and significance testing for Monte Carlo results</done>
</task>

</tasks>

<verification>
Monte Carlo simulation system verification:
- Bootstrap resampling quantifies trade sequence risk
- Confidence intervals provide uncertainty bounds
- Statistical significance testing validates results
- Risk analysis identifies tail behavior and extreme scenarios
</verification>

<success_criteria>
- Monte Carlo simulation engine runs multiple resampled backtests
- Bootstrap resampling tests strategy robustness to trade sequence
- Statistical analysis provides confidence intervals and significance testing
- Risk assessment quantifies tail behavior and extreme scenarios
- Ready for comprehensive performance reporting integration
</success_criteria>

<output>
After completion, create `.planning/phases/09-backtesting-engine/09-03-SUMMARY.md`
</output>