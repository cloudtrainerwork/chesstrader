---
phase: 09-backtesting-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backtesting/core/events.py
  - src/backtesting/core/engine.py
  - src/backtesting/data_handlers/market_data.py
  - src/backtesting/portfolio/portfolio.py
  - src/backtesting/execution/execution.py
autonomous: true
requirements: [BACK-01]

must_haves:
  truths:
    - "User can run a backtest on historical data and get realistic P&L results"
    - "Events are processed sequentially to prevent look-ahead bias"
    - "Portfolio positions are marked-to-market on every tick"
    - "Order execution includes realistic slippage and commissions"
  artifacts:
    - path: "src/backtesting/core/engine.py"
      provides: "Event-driven backtesting engine"
      min_lines: 150
    - path: "src/backtesting/core/events.py"
      provides: "Event system (Market, Signal, Order, Fill events)"
      exports: ["EventType", "MarketEvent", "SignalEvent", "OrderEvent", "FillEvent"]
    - path: "src/backtesting/portfolio/portfolio.py"
      provides: "Portfolio state management with time series tracking"
      exports: ["Portfolio"]
    - path: "src/backtesting/execution/execution.py"
      provides: "Order execution simulation with slippage"
      exports: ["ExecutionHandler"]
  key_links:
    - from: "src/backtesting/core/engine.py"
      to: "src/backtesting/core/events.py"
      via: "event queue processing"
      pattern: "events\\.get.*EventType"
    - from: "src/backtesting/portfolio/portfolio.py"
      to: "market data"
      via: "mark-to-market updates"
      pattern: "update_market_value"
    - from: "src/backtesting/execution/execution.py"
      to: "FillEvent creation"
      via: "order simulation"
      pattern: "FillEvent.*commission.*slippage"
---

<objective>
Build event-driven backtesting engine with realistic order execution and portfolio tracking

Purpose: Create the core infrastructure for historically validating the complete options trading system without look-ahead bias
Output: Event-driven backtesting engine capable of processing market data sequentially and simulating realistic trading
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-backtesting-engine/09-RESEARCH.md

# Phase 8 outputs that we integrate with
@.planning/phases/08-position-manager/08-02-SUMMARY.md
</context>

<interfaces>
<!-- Key interfaces from prior phases that executor needs -->

From Phase 8 Position Manager:
```python
# Available trained position management system
class PositionManagerTrainer:
    def load_trained_model(self, checkpoint_path: str) -> PositionManagerNetwork

# Action types for position decisions
class ActionType(Enum):
    HOLD = 0
    CLOSE = 1
    ADJUST = 2
    ROLL = 3
```

From Phase 5 Strategy Selector:
```python
# Strategy recommendation interface
class RecommendationEngine:
    def get_recommendations(self, state: dict) -> List[StrategyRecommendation]

class StrategyRecommendation:
    strategy_class: str
    confidence: float
    kelly_fraction: float
    expected_return: float
```

From Phase 3 Strategy Framework:
```python
# Base strategy interface
class BaseStrategy:
    def enter_position(self, market_data: dict) -> dict
    def calculate_pnl(self, market_data: dict) -> float
    def get_risk_metrics(self) -> dict
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create event system and core engine</name>
  <files>src/backtesting/core/__init__.py, src/backtesting/core/events.py, src/backtesting/core/engine.py</files>
  <behavior>
    - Event queue processes MarketEvent, SignalEvent, OrderEvent, FillEvent in sequence
    - Engine maintains event queue and coordinates component interactions
    - Events are processed one at a time preventing look-ahead bias
    - Engine can run full backtest from start to end date
  </behavior>
  <action>
    Create event-driven architecture following research patterns. Implement EventType enum with MARKET, SIGNAL, ORDER, FILL. Create concrete event classes with timestamp, symbol, and event-specific data. Build BacktestEngine class with event queue and component coordination. Engine should process events sequentially, dispatch to appropriate handlers, and maintain simulation state. Use queue.Queue for event processing and ensure strict temporal ordering of events.
  </action>
  <verify>
    <automated>python -m pytest tests/backtesting/test_events.py::test_event_processing -x</automated>
  </verify>
  <done>Event system created with sequential processing and BacktestEngine coordinates all components</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create portfolio state management</name>
  <files>src/backtesting/portfolio/__init__.py, src/backtesting/portfolio/portfolio.py</files>
  <behavior>
    - Portfolio tracks positions, cash, commissions, and total equity over time
    - Mark-to-market updates on every MarketEvent
    - Time series equity curve generation
    - Position tracking by symbol with quantity and average cost
  </behavior>
  <action>
    Implement Portfolio class following research pattern. Track current_positions dict (symbol -> quantity), current_holdings dict with cash/commission/total, and all_holdings list for time series. Implement update_market_value method that marks positions to market on each tick and updates total portfolio value. Add methods for position updates, cash management, and equity curve generation. Store complete trading history for performance analysis.
  </action>
  <verify>
    <automated>python -m pytest tests/backtesting/test_portfolio.py::test_portfolio_tracking -x</automated>
  </verify>
  <done>Portfolio tracks positions and equity with continuous mark-to-market updates</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create execution handler with realistic costs</name>
  <files>src/backtesting/execution/__init__.py, src/backtesting/execution/execution.py, src/backtesting/data_handlers/__init__.py, src/backtesting/data_handlers/market_data.py</files>
  <behavior>
    - Order execution includes slippage (0.05-0.1% for options)
    - Commission calculation based on contract size and rates
    - FillEvent generation with realistic fill prices
    - Market data handler provides historical price data
  </behavior>
  <action>
    Create ExecutionHandler class that simulates realistic order fills. Include slippage_rate (default 0.0005 for options), commission_rate configuration. Implement execute_order method that applies slippage based on order direction (BUY adds slippage, SELL subtracts), calculates commission, and returns FillEvent with fill_cost and commission. Create MarketDataHandler for historical data access using yfinance integration. Ensure execution simulation matches real trading costs from research.
  </action>
  <verify>
    <automated>python -m pytest tests/backtesting/test_execution.py::test_realistic_fills -x</automated>
  </verify>
  <done>Order execution simulates realistic slippage and commissions with historical data access</done>
</task>

</tasks>

<verification>
Full backtesting pipeline verification:
- Event-driven processing prevents look-ahead bias
- Portfolio equity curve tracks realistic trading performance
- Order execution costs match real trading expectations
- Market data integration provides historical price feeds
</verification>

<success_criteria>
- Complete event-driven backtesting engine operational
- Portfolio state tracking with continuous marking-to-market
- Realistic order execution with slippage and commission modeling
- Historical market data integration functional
- Ready for strategy integration and signal generation
</success_criteria>

<output>
After completion, create `.planning/phases/09-backtesting-engine/09-01-SUMMARY.md`
</output>