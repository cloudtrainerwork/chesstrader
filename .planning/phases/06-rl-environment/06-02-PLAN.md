---
phase: 06-rl-environment
plan: 02
type: execute
---

<objective>
Implement strategy-specific reward functions that guide RL training toward profitable position management.

Purpose: Create sophisticated reward shaping that accounts for different strategy characteristics, risk profiles, and market conditions to train effective position management policies.
Output: Modular reward system with strategy-specific calculations and proper reward scaling.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Previous plan dependency
@.planning/phases/06-rl-environment/06-01-SUMMARY.md

# Strategy framework for reward design
@src/strategies/base.py
@src/strategies/neutral.py
@src/strategies/directional.py
@src/strategies/volatility.py

**Tech stack available:** Python, PyTorch, OpenAI Gym, NumPy
**Established patterns:** BaseStrategy interface, strategy categories (neutral, directional, volatility)
**Constraining decisions:**
- Project: Target 60%+ win rate with <15% drawdown
- Phase 5-02: Kelly criterion position sizing
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reward calculator base class and P/L rewards</name>
  <files>src/environments/rewards.py, tests/environments/test_rewards.py</files>
  <action>Create RewardCalculator base class with calculate_reward(position_state, action, market_data) method. Implement immediate P/L reward component that returns realized P/L on CLOSE, unrealized P/L change on HOLD/ADJUST, and adjustment cost penalty on ROLL. Add risk-adjusted returns using Sharpe ratio scaling (reward / volatility). Include position size normalization to prevent reward magnitude issues. Add drawdown penalty that triggers when unrealized loss exceeds 10% (multiply reward by 0.5 if in drawdown).</action>
  <verify>python -c "from src.environments.rewards import RewardCalculator; calc = RewardCalculator(); reward = calc.calculate_reward({'pnl': 100}, 'CLOSE', {}); assert isinstance(reward, float)"</verify>
  <done>Reward calculator computes P/L-based rewards with risk adjustment and drawdown penalties</done>
</task>

<task type="auto">
  <name>Task 2: Implement strategy-specific reward components</name>
  <files>src/environments/rewards.py, src/environments/strategy_rewards.py, tests/environments/test_strategy_rewards.py</files>
  <action>Create strategy-specific reward calculators inheriting from RewardCalculator. For NEUTRAL strategies (Iron Condor, Butterfly): add theta decay bonus (positive reward for time passing in profit zone), breach penalty (negative reward if price moves outside strikes). For DIRECTIONAL strategies: add momentum alignment bonus (reward for holding when price moves favorably), early exit penalty (small negative for closing profitable positions too early). For VOLATILITY strategies: add volatility capture reward (bonus when implied vol moves favorably), time decay penalty for long premium strategies. Each strategy category gets its own reward shaping coefficients.</action>
  <verify>python -c "from src.environments.strategy_rewards import NeutralStrategyReward, DirectionalStrategyReward; n = NeutralStrategyReward(); d = DirectionalStrategyReward(); assert hasattr(n, 'calculate_theta_bonus')"</verify>
  <done>Strategy-specific reward components implemented for all three strategy categories</done>
</task>

<task type="auto">
  <name>Task 3: Create reward scaling and normalization system</name>
  <files>src/environments/rewards.py, src/environments/reward_scaler.py, tests/environments/test_reward_scaler.py</files>
  <action>Implement RewardScaler class using running statistics for online normalization. Track running mean and variance of rewards using Welford's algorithm for numerical stability. Apply z-score normalization with clipping at [-3, 3] standard deviations to prevent extreme values. Add reward shaping multipliers: exploration bonus (small positive for trying different actions early in training), convergence decay (reduce exploration bonus over episodes). Include temperature parameter for controlling reward sensitivity during training. Integrate scaler into main environment to ensure consistent reward magnitudes across different market conditions.</action>
  <verify>python -c "from src.environments.reward_scaler import RewardScaler; scaler = RewardScaler(); scaled = scaler.scale_reward(100.0); assert -3 <= scaled <= 3"</verify>
  <done>Reward scaling system normalizes rewards and handles online statistics updates</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `python -m pytest tests/environments/test_rewards.py -v` passes
- [ ] Rewards properly scaled between reasonable bounds
- [ ] Strategy-specific components differentiate behaviors
- [ ] Risk adjustments working (Sharpe scaling, drawdown penalties)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Reward functions incentivize profitable actions
- Strategy-specific rewards reflect trading logic
- Proper normalization prevents training instability
  </success_criteria>

<output>
After completion, create `.planning/phases/06-rl-environment/06-02-SUMMARY.md`:

# Phase 6 Plan 2: Strategy-Specific Reward Functions Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Reward calculator implementation]
- [Strategy-specific components]
- [Scaling system outcome]

## Files Created/Modified

- `src/environments/rewards.py` - Base reward calculator
- `src/environments/strategy_rewards.py` - Strategy-specific rewards
- `src/environments/reward_scaler.py` - Normalization system

## Decisions Made

[Reward shaping decisions, scaling approaches, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 06-03-PLAN.md (Episode management and terminal conditions)
</output>