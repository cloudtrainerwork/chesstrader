---
phase: 06-rl-environment
plan: 03
type: execute
---

<objective>
Implement episode management system with terminal conditions and environment integration.

Purpose: Complete the RL environment with proper episode lifecycle management, terminal conditions for different scenarios, and integration of all components into a cohesive training environment.
Output: Fully functional Gym environment ready for PPO training with realistic episode dynamics.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Previous plan dependencies
@.planning/phases/06-rl-environment/06-01-SUMMARY.md
@.planning/phases/06-rl-environment/06-02-SUMMARY.md

# Environment components to integrate
@src/environments/base.py
@src/environments/rewards.py
@src/environments/actions.py

**Tech stack available:** Python, PyTorch, OpenAI Gym, NumPy
**Established patterns:** Environment base class, reward system, action space
**Constraining decisions:**
- Project: Position management with hold/close/adjust/roll decisions
- Target: Episodes should reflect realistic trading scenarios
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement episode lifecycle and terminal conditions</name>
  <files>src/environments/episode.py, src/environments/base.py, tests/environments/test_episode.py</files>
  <action>Create EpisodeManager class handling episode lifecycle. Define terminal conditions: position closed (action=CLOSE executed), expiration reached (days to expiry <= 1), max loss exceeded (unrealized loss > 20%), max steps reached (default 252 trading days). Implement is_terminal(state) checking all conditions. Add episode statistics tracking: total P/L, number of adjustments, max drawdown, win/loss flag. Update OptionsEnvironment.step() to use EpisodeManager for done flag determination. Include early stopping bonus in terminal rewards (small positive if profitable when stopped).</action>
  <verify>python -c "from src.environments.episode import EpisodeManager; em = EpisodeManager(); done = em.is_terminal({'days_to_expiry': 0}); assert done == True"</verify>
  <done>Episode manager handles all terminal conditions and tracks statistics correctly</done>
</task>

<task type="auto">
  <name>Task 2: Create market data simulator for episodes</name>
  <files>src/environments/market_sim.py, tests/environments/test_market_sim.py</files>
  <action>Implement MarketDataSimulator class providing price movements during episodes. Use geometric Brownian motion for realistic price paths with configurable drift and volatility. Add regime-aware simulation adjusting parameters based on market regime (trending/volatile/calm). Include option Greeks updates using Black-Scholes for each step. Support historical data replay mode loading actual price sequences from saved data. Add volatility surface simulation for realistic IV changes. Ensure reproducibility with seed parameter for training consistency.</action>
  <verify>python -c "from src.environments.market_sim import MarketDataSimulator; sim = MarketDataSimulator(seed=42); price_path = sim.generate_episode(100, 0.2, 20); assert len(price_path) == 20"</verify>
  <done>Market simulator generates realistic price paths and Greeks updates</done>
</task>

<task type="auto">
  <name>Task 3: Integrate components into complete training environment</name>
  <files>src/environments/options_env.py, src/environments/__init__.py, tests/environments/test_integration.py</files>
  <action>Create OptionsTrainingEnvironment combining all components. Initialize with strategy type, market simulator, reward calculator, and episode manager. Implement full reset() creating new position with random market conditions. Update step() to: get market update from simulator, calculate position P/L, determine reward using strategy-specific calculator, check terminal conditions, update observation. Add render() method showing position state, P/L, and current action. Include seed() method for reproducible training. Create make_env() factory function for easy environment creation with different configurations.</action>
  <verify>python -c "from src.environments import make_env; env = make_env('IronCondor'); obs = env.reset(); for _ in range(10): obs, r, done, info = env.step(env.action_space.sample()); if done: break"</verify>
  <done>Complete environment runs full episodes with all components integrated</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `python -m pytest tests/environments/ -v` passes all tests
- [ ] Environment runs complete episodes without errors
- [ ] Terminal conditions trigger appropriately
- [ ] Market simulation produces realistic scenarios
- [ ] Full Gym API compliance verified
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Environment ready for RL training
- Episodes reflect realistic trading scenarios
- All components properly integrated
- Phase 6 complete
  </success_criteria>

<output>
After completion, create `.planning/phases/06-rl-environment/06-03-SUMMARY.md`:

# Phase 6 Plan 3: Episode Management and Integration Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Episode management outcome]
- [Market simulation outcome]
- [Integration outcome]

## Files Created/Modified

- `src/environments/episode.py` - Episode lifecycle management
- `src/environments/market_sim.py` - Market data simulator
- `src/environments/options_env.py` - Complete training environment

## Decisions Made

[Terminal conditions, simulation parameters, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Phase Status

✅ **Phase 6: RL Environment COMPLETE**

OpenAI Gym-compatible environment implemented with observation/action spaces, strategy-specific rewards, and episode management. Ready for Phase 7: PPO Training.
</output>