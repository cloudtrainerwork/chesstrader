---
phase: 06-rl-environment
plan: 01
type: execute
---

<objective>
Create OpenAI Gym-compatible environment base class with observation and action spaces for options position management.

Purpose: Establish the foundational RL environment structure that PPO training will interact with, defining how the agent observes market state and what actions it can take.
Output: Working Gym environment with proper observation/action spaces and basic step mechanics.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 3 dependency - Strategy framework
@.planning/phases/03-strategy-framework/03-01-SUMMARY.md
@.planning/phases/03-strategy-framework/03-04-SUMMARY.md

# Core strategy framework to integrate
@src/strategies/base.py
@src/strategies/factory.py

**Tech stack available:** Python, PyTorch, OpenAI Gym
**Established patterns:** 16 core strategy types, BaseStrategy interface, position states
**Constraining decisions:**
- Phase 3: Complete strategy framework with standardized interface
- Project: RL-based position management (hold, close, adjust, roll)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create base environment class with observation space</name>
  <files>src/environments/__init__.py, src/environments/base.py, tests/environments/test_base.py</files>
  <action>Create OptionsEnvironment base class inheriting from gym.Env. Define observation_space as gym.spaces.Box with shape matching position state features: price movements (10 dims), option Greeks (5 dims), position metrics (5 dims), time decay (2 dims), volatility (3 dims), regime indicators (5 dims) = 30 total dimensions. Include position_state property holding current strategy, strikes, quantities. Implement reset() returning initial observation. Use numpy arrays for observations to ensure Gym compatibility.</action>
  <verify>python -c "import gym; from src.environments.base import OptionsEnvironment; env = OptionsEnvironment(); obs = env.reset(); assert obs.shape == (30,)"</verify>
  <done>Environment instantiates, observation space defined correctly, reset returns proper shaped observation</done>
</task>

<task type="auto">
  <name>Task 2: Define action space and step function</name>
  <files>src/environments/base.py, src/environments/actions.py, tests/environments/test_actions.py</files>
  <action>Define action_space as gym.spaces.Discrete(4) for [HOLD, CLOSE, ADJUST, ROLL] actions. Create ActionType enum matching these. Implement step(action) that accepts action index, updates position state based on action, calculates immediate reward (placeholder for now), determines if episode done (position closed or max steps), returns (observation, reward, done, info). Add position adjustment logic stubs for ADJUST (change strikes/quantities) and ROLL (move to next expiration). Track cumulative P/L in info dict.</action>
  <verify>python -c "from src.environments.base import OptionsEnvironment; env = OptionsEnvironment(); env.reset(); obs, reward, done, info = env.step(0); assert obs.shape == (30,); assert isinstance(reward, float)"</verify>
  <done>Step function accepts actions, returns proper tuple format, action space correctly defined</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `python -m pytest tests/environments/ -v` passes
- [ ] Environment follows OpenAI Gym API conventions
- [ ] Observation and action spaces properly defined
- [ ] Reset and step functions work correctly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Gym environment instantiates and runs basic episode
- Observation space matches position state requirements
- Action space covers core position management decisions
  </success_criteria>

<output>
After completion, create `.planning/phases/06-rl-environment/06-01-SUMMARY.md`:

# Phase 6 Plan 1: Environment Base Class Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Base environment class outcome]
- [Observation/action space outcome]

## Files Created/Modified

- `src/environments/base.py` - Core environment class
- `src/environments/actions.py` - Action definitions
- `tests/environments/test_base.py` - Environment tests

## Decisions Made

[Key environment design decisions, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 06-02-PLAN.md (Strategy-specific reward functions)
</output>