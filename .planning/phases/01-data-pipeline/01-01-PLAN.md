---
phase: 01-data-pipeline
plan: 01
type: execute
---

<objective>
Set up project foundation with data provider integration and caching layer.

Purpose: Establish the core infrastructure for fetching and storing market data efficiently.
Output: Working data pipeline with yfinance integration and SQLite caching.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-phase.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-pipeline/DISCOVERY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up project structure and dependencies</name>
  <files>README.md, requirements.txt, src/__init__.py, src/config.py, src/data/__init__.py, tests/__init__.py</files>
  <action>Create project structure following Python best practices. Set up requirements.txt with core dependencies: yfinance>=0.2.28, pandas>=2.0.0, numpy>=1.24.0, pandas-ta>=0.3.14b0, sqlalchemy>=2.0.0, python-dotenv>=1.0.0, tenacity>=8.2.0, pytest>=7.4.0. Create config.py with settings for data sources, cache paths, and API limits. Use dataclasses for configuration. Initialize git repository with .gitignore for Python (include .env, *.db, __pycache__).</action>
  <verify>python -m pytest --version succeeds, python -c "import yfinance, pandas, pandas_ta" works without errors</verify>
  <done>Project structure created, all dependencies installable, imports work</done>
</task>

<task type="auto">
  <name>Task 2: Implement data fetcher with yfinance wrapper</name>
  <files>src/data/providers.py, src/data/models.py, tests/test_providers.py</files>
  <action>Create abstract DataProvider interface with methods: get_price_history(symbol, start, end), get_current_price(symbol), get_options_chain(symbol). Implement YFinanceProvider with tenacity retry decorator (3 retries, exponential backoff). Add rate limiting (max 30 requests/minute). Create PriceData and OptionsData dataclasses. Include comprehensive error handling for network issues, invalid symbols, and missing data. Implement batch fetching for multiple symbols. Never use raw yfinance directly - always through the wrapper.</action>
  <verify>Run test script that fetches SPY data for last 30 days, handles invalid symbol gracefully</verify>
  <done>YFinanceProvider fetches data reliably with retry logic and error handling</done>
</task>

<task type="auto">
  <name>Task 3: Build SQLite caching layer</name>
  <files>src/data/cache.py, src/data/schema.py, tests/test_cache.py</files>
  <action>Implement CacheManager using SQLAlchemy ORM. Create tables: price_history (symbol, date, open, high, low, close, volume, updated_at), options_data (symbol, expiry, strike, option_type, bid, ask, iv, greeks_json, updated_at). Add cache invalidation (24 hours for prices, 15 min for options). Implement get_or_fetch pattern - check cache first, fetch if stale/missing. Add cache statistics tracking (hits, misses, size). Use SQLAlchemy declarative base, never raw SQL. Store database in data/cache.db.</action>
  <verify>python -c "from src.data.cache import CacheManager; cm = CacheManager(); cm.get_price_history('SPY')" returns data, second call hits cache</verify>
  <done>Cache stores/retrieves data, respects TTL, tracks statistics</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `python -m pytest tests/` passes all tests
- [ ] Can fetch and cache data for SPY, QQQ, IWM
- [ ] Cache hit rate > 90% on repeated requests
- [ ] No hardcoded API keys in code
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Data pipeline handles errors gracefully
- Cache reduces API calls by >90%
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline/01-01-SUMMARY.md`:

# Phase 1 Plan 1: Data Provider Integration Summary

**Established data fetching infrastructure with yfinance and SQLite caching**

## Accomplishments

- Project structure with proper dependency management
- Robust data fetcher with retry logic and rate limiting
- SQLite cache reducing API calls by 90%+

## Files Created/Modified

- `requirements.txt` - Project dependencies
- `src/data/providers.py` - Data provider abstraction
- `src/data/cache.py` - Caching layer implementation

## Decisions Made

- Using yfinance for MVP, abstract for future Polygon migration
- SQLite for local caching, sufficient for backtesting
- 24-hour cache TTL for price data

## Issues Encountered

[Any issues and resolutions]

## Next Step

Ready for 01-02-PLAN.md (Regime State Features)
</output>