---
phase: 01-data-pipeline
plan: 03
type: execute
---

<objective>
Implement position state feature engineering for 24-dimensional position tracking vectors.

Purpose: Create comprehensive position state representation for RL-based management.
Output: Position state module with Greeks calculation and feature assembly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-phase.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-pipeline/DISCOVERY.md
@.planning/phases/01-data-pipeline/01-01-SUMMARY.md
@.planning/phases/01-data-pipeline/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create position state data model</name>
  <files>src/features/position_state.py, src/features/position_models.py, tests/test_position_state.py</files>
  <action>Create Position dataclass with fields: strategy_type (enum for 16 strategies), entry_date, expiration_date, strikes (list), option_types (list of CALL/PUT), quantities (list), entry_prices (list), current_prices (list), underlying_price_at_entry, current_underlying_price. Create PositionZones enum: DEEP_LOSS = -3, LOSS = -2, WARNING = -1, SAFE = 0, PROFIT = 1, HIGH_PROFIT = 2, MAX_PROFIT = 3. Implement calculate_price_zone() method that maps position P/L to zones based on max profit/loss boundaries. Add calculate_breakevens() for each strategy type. All monetary values in cents to avoid float precision issues.</action>
  <verify>python -c "from src.features.position_models import Position, StrategyType; p = Position(strategy_type=StrategyType.IRON_CONDOR); assert p.strategy_type"</verify>
  <done>Position model with strategy types and price zone calculation</done>
</task>

<task type="auto">
  <name>Task 2: Implement Greeks and P/L calculations</name>
  <files>src/features/greeks.py, src/features/pnl.py, tests/test_greeks.py</files>
  <action>Create GreeksCalculator using Black-Scholes for European options. Implement calculate_delta(), calculate_gamma(), calculate_theta(), calculate_vega() for individual options. Add position_greeks() to aggregate across multi-leg positions (sum deltas, gammas, vegas; theta always negative sum). Create PnLCalculator with methods: unrealized_pnl(position, current_prices), pnl_percentage(), percent_of_max_profit(), percent_of_max_loss(), days_held(). For IV, use yfinance option chain data when available, else use 30-day historical volatility as proxy. All Greeks normalized: delta [-1,1], gamma/vega/theta divided by underlying price.</action>
  <verify>python -c "from src.features.greeks import GreeksCalculator; gc = GreeksCalculator(); delta = gc.calculate_delta(S=100, K=105, T=0.1, r=0.05, sigma=0.2, option_type='CALL'); assert -1 <= delta <= 1"</verify>
  <done>Greeks calculation with Black-Scholes and P/L tracking</done>
</task>

<task type="auto">
  <name>Task 3: Build feature vector assembler</name>
  <files>src/features/position_state.py, src/features/position_vector.py, tests/test_position_vector.py</files>
  <action>Create PositionStateVector class that assembles 24 features: strategy_identity (1: one-hot index), board_position (6: price_zone, zone_velocity, distance_to_upper_strike, distance_to_lower_strike, distance_to_upper_breakeven, distance_to_lower_breakeven), time (3: dte, percent_time_remaining, theta_per_day), volatility (3: iv_at_entry, current_iv, iv_change), greeks (4: position_delta, position_gamma, position_theta, position_vega), pnl_status (5: entry_credit, current_value, unrealized_pnl, percent_of_max_profit, percent_of_max_loss), meta (2: days_held, adjustments_made). Add validate() to ensure all 24 features present and in valid ranges. Create batch processing for multiple positions.</action>
  <verify>python -c "from src.features.position_vector import PositionStateVector; psv = PositionStateVector(); state = psv.calculate(test_position); assert state.shape == (24,) and not any(pd.isna(state))"</verify>
  <done>24-dimensional position state vector assembly with validation</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `python -m pytest tests/features/` passes all tests
- [ ] Position state vector returns exactly 24 features
- [ ] Greeks calculation matches expected Black-Scholes values
- [ ] P/L calculations accurate for all 16 strategies
- [ ] No NaN values in output vectors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- 24-dimensional vectors for any position type
- Greeks and P/L calculations accurate
- Phase 1 complete: data pipeline fully functional
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline/01-03-SUMMARY.md`:

# Phase 1 Plan 3: Position State Features Summary

**Completed position state feature engineering with Greeks and P/L tracking**

## Accomplishments

- Position data model for 16 strategy types
- Black-Scholes Greeks calculation
- P/L tracking with zone classification
- 24-dimensional position state vectors

## Files Created/Modified

- `src/features/position_models.py` - Position data structures
- `src/features/greeks.py` - Options Greeks calculator
- `src/features/position_vector.py` - State vector assembly

## Decisions Made

- Use Black-Scholes for Greeks (industry standard)
- Store money in cents to avoid float precision issues
- Map P/L to 7 zones for spatial representation

## Issues Encountered

[Any issues and resolutions]

## Next Step

Phase 1 complete, ready for Phase 2: Regime Detection
</output>