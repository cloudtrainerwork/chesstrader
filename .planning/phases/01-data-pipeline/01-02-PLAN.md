---
phase: 01-data-pipeline
plan: 02
type: execute
---

<objective>
Build regime state feature engineering pipeline for 48-dimensional market state vectors.

Purpose: Transform raw price data into comprehensive features for regime detection.
Output: Feature engineering module producing consistent 48-dim state vectors.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-phase.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-pipeline/DISCOVERY.md
@.planning/phases/01-data-pipeline/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement price structure features (6 dimensions)</name>
  <files>src/features/regime_features.py, src/features/base.py, tests/test_regime_features.py</files>
  <action>Create FeatureEngineering base class with standardize() and validate() methods. Implement PriceStructureFeatures: price_vs_sma20 (price/SMA20 - 1), price_vs_sma50, price_vs_sma200, distance_from_52w_high ((price - 52w_high) / 52w_high), distance_from_52w_low ((price - 52w_low) / 52w_low), gap_percentage ((open - prev_close) / prev_close). Use pandas rolling windows for SMAs. All features should be normalized to roughly [-1, 1] range for neural network compatibility. Handle missing data with forward fill then backward fill.</action>
  <verify>python -c "from src.features.regime_features import PriceStructureFeatures; pf = PriceStructureFeatures(); features = pf.calculate('SPY'); assert features.shape[1] == 6"</verify>
  <done>Price structure features return 6 normalized dimensions for any symbol</done>
</task>

<task type="auto">
  <name>Task 2: Calculate trend and momentum indicators (15 dimensions)</name>
  <files>src/features/regime_features.py, src/features/indicators.py, tests/test_indicators.py</files>
  <action>Add TrendIndicators class: ADX (14-period), +DI, -DI, MACD line, MACD signal, MACD histogram (all normalized by price), EMA alignment score (EMA9 vs EMA21 vs EMA50), higher_highs flag, lower_lows flag. Add MomentumIndicators: RSI (14-period), Stochastic %K, Stochastic %D, ROC_5 (5-day rate of change), ROC_10, ROC_20. Use pandas-ta library for all indicators (never hand-roll). For ADX use pandas_ta.adx(), for MACD use pandas_ta.macd(), etc. Normalize unbounded indicators by dividing by rolling std.</action>
  <verify>python -c "import pandas_ta; df = get_spy_data(); df.ta.adx(); df.ta.macd(); df.ta.rsi()" runs without error</verify>
  <done>15 trend/momentum features calculated using pandas-ta library</done>
</task>

<task type="auto">
  <name>Task 3: Compute volatility and market context features (27 dimensions)</name>
  <files>src/features/regime_features.py, src/features/market_context.py, tests/test_market_context.py</files>
  <action>Add VolatilityFeatures (11 dim): implied_volatility (from options if available), iv_rank (percentile over 252 days), iv_percentile, historical_volatility_20, vix_level, vix_percentile, term_structure_slope, put_call_skew, bollinger_band_width, bollinger_position, atr_normalized. Add VolumeFeatures (3 dim): volume_ratio (vol/20d_avg), obv_slope, volume_trend. Add SupportResistance (6 dim): distance_to_support, support_strength, distance_to_resistance, resistance_strength, consolidation_score, range_width. Add MarketContext (4 dim): spy_correlation, sector_relative_strength, market_breadth, put_call_ratio. Add EventFeatures (3 dim): days_to_earnings, days_to_fomc, days_to_opex. Create RegimeStateVector.assemble() to combine all 48 features.</action>
  <verify>python -c "from src.features.regime_features import RegimeStateVector; rsv = RegimeStateVector(); state = rsv.calculate('SPY'); assert state.shape == (48,)"</verify>
  <done>Complete 48-dimensional regime state vector with all subcategories</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `python -m pytest tests/features/` passes all tests
- [ ] Regime state vector returns exactly 48 features
- [ ] All features normalized to reasonable ranges
- [ ] No NaN values in output vectors
- [ ] Performance: <100ms per symbol
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- 48-dimensional vectors consistent across symbols
- Features match TDD specification exactly
- Uses pandas-ta for all technical indicators
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Regime State Features Summary

**Built complete 48-dimensional regime state feature engineering pipeline**

## Accomplishments

- Price structure features (6 dim) with SMA comparisons
- Trend/momentum indicators (15 dim) using pandas-ta
- Volatility and market context features (27 dim)
- Unified RegimeStateVector assembler

## Files Created/Modified

- `src/features/regime_features.py` - Main feature engineering
- `src/features/indicators.py` - Technical indicator wrappers
- `src/features/market_context.py` - Market microstructure features

## Decisions Made

- Use pandas-ta for all indicators (no custom implementations)
- Normalize features to [-1, 1] for neural network compatibility
- Forward-fill then backward-fill for missing data

## Issues Encountered

[Any issues and resolutions]

## Next Step

Ready for 01-03-PLAN.md (Position State Features)
</output>