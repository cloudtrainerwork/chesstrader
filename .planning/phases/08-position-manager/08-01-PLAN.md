---
phase: 08-position-manager
plan: 01
type: execute
---

<objective>
Design and implement actor-critic neural network architecture for intelligent position management in options trading, building on the chess-inspired spatial encoder patterns and integrating with the existing PPO training infrastructure.

Purpose: Create the core neural network that will make position management decisions (hold, close, adjust, roll) based on market state, position state, and regime information.
Output: Production-ready actor-critic network with comprehensive testing and evaluation capabilities.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-phase.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Auto-selected based on dependency graph:
@.planning/phases/07-ppo-training/07-01-SUMMARY.md
@.planning/phases/07-ppo-training/07-03-SUMMARY.md
@.planning/phases/06-rl-environment/06-03-SUMMARY.md
@.planning/phases/04-neural-architecture/04-02-SUMMARY.md

# Key files from prior phases:
@src/training/ppo/networks.py
@src/training/ppo/algorithm.py
@src/models/neural/spatial_encoder.py
@src/environments/base.py

**Tech stack available:** PyTorch, gym, chess-inspired architectures, PPO algorithm, GAE, curriculum learning
**Established patterns:** Spatial encoding, residual blocks, attention mechanisms, actor-critic networks, orthogonal initialization

**Constraining decisions:**
- Phase 06-01: 35-dimensional observation space for comprehensive state representation
- Phase 06-01: 4 discrete actions (HOLD, CLOSE, ADJUST, ROLL)
- Phase 04-02: Chess-inspired conv layers with residual blocks and attention
- Phase 07-01: Actor-Critic networks with shared feature extraction
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design Position Manager Network Architecture</name>
  <files>src/models/position_manager.py</files>
  <action>Create PositionManagerNetwork class that extends the actor-critic pattern from PPO networks but specialized for position management decisions. Use 35-dimensional input (market state + position state + episode state). Actor outputs 4-dimensional action probabilities for [HOLD, CLOSE, ADJUST, ROLL]. Critic outputs position value estimate. Include shared feature extraction layers [256, 128, 64] with ReLU activation, residual connections, and attention mechanism inspired by chess architecture. Use orthogonal initialization for final layers to ensure stable training convergence.</action>
  <verify>python -c "from src.models.position_manager import PositionManagerNetwork; net = PositionManagerNetwork(); print(f'Input shape: (35,) -> Actor: {net.actor.output_size}, Critic: {net.critic.output_size}')"</verify>
  <done>Network accepts 35-dim input, outputs 4-dim action logits and scalar value estimate, architecture follows established patterns</done>
</task>

<task type="auto">
  <name>Task 2: Implement Position-Aware Action Selection</name>
  <files>src/models/position_manager.py</files>
  <action>Add position-aware action masking and selection logic to PositionManagerNetwork. Implement get_action() method that handles deterministic/stochastic action selection with temperature control. Add action masking for invalid actions (e.g., cannot CLOSE if no position, cannot ADJUST beyond position limits). Include position state validation and risk constraint checking. Add action probability calculation with entropy bonus for exploration vs exploitation balance. Ensure integration with existing PPO action selection patterns.</action>
  <verify>python -c "from src.models.position_manager import PositionManagerNetwork; import torch; net = PositionManagerNetwork(); obs = torch.randn(35); action, log_prob = net.get_action(obs); print(f'Action: {action.item()}, Log prob: {log_prob.item():.4f}')"</verify>
  <done>Action selection works with masking, returns valid actions with log probabilities, handles edge cases</done>
</task>

<task type="auto">
  <name>Task 3: Add Position State Encoding</name>
  <files>src/models/position_manager.py</files>
  <action>Implement specialized position state encoder that processes the position components of the 35-dimensional observation space. Create PositionEncoder class that takes position-specific features (strike prices, time to expiration, current P&L, Greeks) and creates dense embeddings. Use attention mechanism to focus on most relevant position attributes given current market regime. Include position risk assessment (max drawdown, Greeks exposure) and position complexity encoding (number of legs, spread width). Integrate with main network architecture as a preprocessing module.</action>
  <verify>python -c "from src.models.position_manager import PositionEncoder; import torch; encoder = PositionEncoder(); pos_state = torch.randn(12); encoded = encoder(pos_state); print(f'Position state {pos_state.shape} -> encoded {encoded.shape}')"</verify>
  <done>Position encoder processes position features, outputs meaningful embeddings, integrates with main network</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `python -m pytest tests/models/test_position_manager.py -v` passes all tests
- [ ] Network architecture matches specified dimensions and patterns
- [ ] Action selection handles all edge cases and masking correctly
- [ ] Position encoding integrates properly with existing patterns
- [ ] No import errors or missing dependencies
</verification>

<success_criteria>

- PositionManagerNetwork implemented with actor-critic architecture
- Action selection with proper masking and probability calculation
- Position state encoding with attention and risk assessment
- Integration with existing neural architecture patterns
- Comprehensive test coverage (>90%)
- All verification checks pass

</success_criteria>

<output>
After completion, create `.planning/phases/08-position-manager/08-01-SUMMARY.md`:

# Phase 8 Plan 1: Position Manager Network Architecture Summary

**Actor-critic network for intelligent options position management with specialized encoding and action selection**

## Accomplishments

- PositionManagerNetwork with 35-dim input → 4-action output + value estimate
- Position-aware action masking and selection with risk constraints
- Specialized position state encoding with attention mechanism
- Integration with chess-inspired architecture patterns from Phase 4

## Files Created/Modified

- `src/models/position_manager.py` - Main position manager network architecture
- `tests/models/test_position_manager.py` - Comprehensive test suite

## Decisions Made

[Key architectural decisions and rationale]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 08-02-PLAN.md - Integration with PPO trainer
</output>