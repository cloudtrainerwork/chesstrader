---
phase: 02-regime-detection
type: execute
---

<objective>
Prepare training data for regime detection neural network with historical labeling.

Purpose: Create comprehensive training dataset by collecting historical market data, labeling regime periods, and assembling feature vectors for neural network training.
Output: Labeled training dataset with regime classifications and properly formatted feature vectors ready for model training.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Dependencies:
@.planning/phases/02-regime-detection/02-01-SUMMARY.md
@src/models/regime_detector.py
@src/features/regime_features.py
@src/data/providers.py

**Tech stack available:**
- Python with numpy, pandas, scikit-learn, PyTorch
- YFinance data provider with SQLite caching
- 48-dimensional regime feature engineering pipeline
- RegimeDetector neural network architecture

**Established patterns:**
- Data provider abstraction with caching
- Feature engineering with [-1, 1] normalization
- Comprehensive validation and testing
- Type hints and dataclass patterns

**8-regime classification system:**
1. Bull Trending - Strong upward momentum
2. Bear Trending - Strong downward momentum
3. High Volatility - Elevated volatility environment
4. Low Volatility - Subdued volatility environment
5. Sideways/Ranging - Consolidation patterns
6. Recovery - Post-decline bounce patterns
7. Distribution - Pre-decline weakening
8. Crisis - Extreme stress conditions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Historical market data collection and regime labeling</name>
  <files>src/data/regime_labeler.py, src/data/training_data.py</files>
  <action>Create regime labeling system that analyzes historical SPY data (5+ years) and assigns regime classifications based on market characteristics. Use quantitative rules: Bull (price > SMA50, RSI > 55, low volatility), Bear (price < SMA50, RSI < 45, high volatility), High Vol (VIX > 25 or rolling vol > 30%), Low Vol (VIX < 15 and stable), Sideways (price oscillating around SMA20/50), Recovery (strong bounce from oversold), Distribution (weakness after strength), Crisis (extreme drawdown periods). Create TrainingDataCollector class to fetch SPY historical data and apply regime labels. Use 252-day rolling windows for regime stability.</action>
  <verify>python -c "from src.data.regime_labeler import RegimeLabeler; labeler = RegimeLabeler(); labels = labeler.label_historical_data('SPY'); print(f'Labeled {len(labels)} periods')" shows successful labeling</verify>
  <done>Historical data collected and labeled with 8-regime classifications, RegimeLabeler validates label distribution</done>
</task>

<task type="auto">
  <name>Task 2: Feature vector assembly and validation</name>
  <files>src/data/training_data.py, tests/data/test_training_data.py</files>
  <action>Create TrainingDataset class that combines regime features with regime labels into training-ready format. For each labeled time period, calculate 48D regime features using RegimeStateVector and pair with regime label. Ensure feature-label temporal alignment (features at time T predict regime at time T). Validate feature ranges [-1, 1], check for NaN/inf values, verify regime label distribution is reasonable (no regime dominates >40%). Create train/val/test splits (70/15/15) with temporal ordering preserved. Add comprehensive tests covering data integrity, feature-label alignment, and split validation.</action>
  <verify>pytest tests/data/test_training_data.py -v passes, python -c "from src.data.training_data import TrainingDataset; ds = TrainingDataset(); print(f'Dataset: {len(ds)} samples, features: {ds.features.shape}, labels: {ds.labels.shape}')" shows correct shapes</verify>
  <done>Training dataset assembled with 48D features paired to regime labels, data validation confirms quality and temporal consistency</done>
</task>

<task type="auto">
  <name>Task 3: Data preprocessing and augmentation pipeline</name>
  <files>src/data/training_data.py, src/data/data_utils.py</files>
  <action>Add data preprocessing pipeline with feature standardization verification, sequence padding for batch training, and light data augmentation (5% noise injection for robustness). Create DataLoader integration with PyTorch for efficient batch training. Implement regime balancing to handle class imbalance - use stratified sampling to ensure all 8 regimes are represented in each batch. Add feature importance analysis to identify most discriminative dimensions. Create data statistics reporting (regime frequency, feature distributions, correlation matrix). Avoid heavy augmentation that could distort market relationships.</action>
  <verify>python -c "from src.data.training_data import create_data_loaders; train_dl, val_dl, test_dl = create_data_loaders(); print(f'Data loaders created: train={len(train_dl)}, val={len(val_dl)}, test={len(test_dl)}')" succeeds without errors</verify>
  <done>Data preprocessing pipeline complete with balanced sampling, PyTorch DataLoaders ready, augmentation and validation integrated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Historical data covers 5+ years with all 8 regimes represented
- [ ] Feature vectors maintain [-1, 1] normalization from Phase 1
- [ ] No regime label dominates >40% of dataset
- [ ] Train/val/test splits maintain temporal ordering
- [ ] PyTorch DataLoaders produce correct batch shapes (N, 48) features, (N,) labels
</verification>

<success_criteria>

- All tasks completed successfully
- Historical SPY data collected and regime-labeled over 5+ year period
- 48D feature vectors properly assembled and validated
- Training dataset split with temporal consistency preserved
- PyTorch DataLoaders ready for neural network training
- Data quality validated with comprehensive tests
- Ready for training loop implementation in 02-03-PLAN.md
</success_criteria>

<output>
After completion, create `.planning/phases/02-regime-detection/02-02-SUMMARY.md`:

# Phase 2 Plan 2: Training Data Preparation Summary

**Prepared comprehensive training dataset with regime labeling and feature assembly**

## Accomplishments

- Historical market data collection and regime labeling system
- Feature vector assembly with 48D regime features
- Training/validation/test data splits with temporal consistency

## Files Created/Modified

- `src/data/regime_labeler.py` - Regime classification logic
- `src/data/training_data.py` - Dataset assembly and preprocessing
- `src/data/data_utils.py` - Utility functions and DataLoader integration
- `tests/data/test_training_data.py` - Comprehensive test coverage

## Decisions Made

[Key decisions about regime labeling rules and data splits, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 02-03-PLAN.md: Training loop implementation and validation
</output>