---
phase: 02-regime-detection
type: execute
---

<objective>
Implement comprehensive training loop and validation system for regime detection neural network.

Purpose: Create a complete training infrastructure with proper loss functions, validation metrics, early stopping, model checkpointing, and monitoring for the regime detection system. Integrate all components from phases 02-01 and 02-02 into a production-ready training pipeline.
Output: Trained regime detection model with comprehensive validation metrics, saved checkpoints, and training visualization ready for integration.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Dependencies:
@.planning/phases/02-regime-detection/02-01-SUMMARY.md
@.planning/phases/02-regime-detection/02-02-SUMMARY.md
@src/models/regime_detector.py
@src/data/training_data.py
@src/data/regime_labeler.py
@src/features/regime_features.py

**Tech stack available:**
- Python with numpy, pandas, scikit-learn, PyTorch
- RegimeDetector neural network architecture (48→128→64→32→8+1)
- TrainingDataCollector with balanced sampling and temporal splits
- RegimeDataset with PyTorch DataLoaders
- Existing config management system

**Established patterns:**
- Comprehensive validation and testing approach
- Configuration management with dataclass patterns
- Error handling and logging with structured approach
- Type hints throughout codebase
- Device-agnostic PyTorch implementation

**Training requirements:**
- Multi-class classification (8 regimes) with confidence regression
- Combined loss function (classification + confidence)
- Temporal validation preserving chronological order
- Model checkpointing for best performance and recovery
- Early stopping to prevent overfitting
- Comprehensive metrics tracking and visualization
- Device management (CPU/GPU) with automatic detection
</context>

<tasks>

<task type="auto">
  <name>Task 1: Training configuration and hyperparameters</name>
  <files>src/config.py</files>
  <action>Add TrainingConfig dataclass to config.py with all training hyperparameters: learning_rate (1e-3), weight_decay (1e-4), batch_size (32), max_epochs (100), patience (10), min_delta (1e-4), loss_weights (classification: 0.8, confidence: 0.2), lr_scheduler settings (factor: 0.5, patience: 5, min_lr: 1e-6), device management (auto-detect CUDA), checkpoint settings, and validation frequency. Include gradient clipping threshold (1.0) and mixed precision training flag. Update main Config class to include training: TrainingConfig field.</action>
  <verify>python -c "from src.config import config; print(f'Learning rate: {config.training.learning_rate}'); print(f'Loss weights: {config.training.loss_weights}')" executes without errors and shows correct values</verify>
  <done>Training configuration added to config.py with comprehensive hyperparameters</done>
</task>

<task type="auto">
  <name>Task 2: Core training infrastructure implementation</name>
  <files>src/training/__init__.py, src/training/trainer.py</files>
  <action>Create training module and RegimeTrainer class. Implement constructor accepting model and config, device management with automatic CUDA detection. Add combined loss function using CrossEntropyLoss for regime classification and MSELoss for confidence regression with configurable weights. Implement training step with gradient clipping, validation step without gradients, and epoch loop with comprehensive metrics tracking. Include learning rate scheduler (ReduceLROnPlateau) and early stopping logic with patience mechanism. Add methods for saving/loading training state and model checkpoints. Use mixed precision training with GradScaler for efficiency while maintaining numerical stability.</action>
  <verify>python -c "from src.training.trainer import RegimeTrainer; from src.models.regime_detector import RegimeDetector; model = RegimeDetector(); trainer = RegimeTrainer(model); print('Trainer initialized successfully')" succeeds without import errors</verify>
  <done>RegimeTrainer implemented with complete training infrastructure and device management</done>
</task>

<task type="auto">
  <name>Task 3: Validation metrics and monitoring system</name>
  <files>src/training/trainer.py, src/training/metrics.py</files>
  <action>Create comprehensive validation metrics system in RegimeTrainer and separate metrics.py module. Implement classification metrics: overall accuracy, per-regime precision/recall/F1, confusion matrix, and balanced accuracy. Add confidence calibration analysis: calibration plot data, expected calibration error (ECE), reliability diagram components. Include temporal stability metrics: prediction smoothness over time windows and regime transition analysis. Add training progress tracking: loss curves, learning rate history, and validation metrics over epochs. Implement metric logging with structured format and optional visualization data export. Include methods for metric aggregation and comparison across epochs.</action>
  <verify>python -c "from src.training.metrics import calculate_regime_metrics, calculate_calibration_metrics; import torch; pred = torch.rand(10, 8); conf = torch.rand(10, 1); labels = torch.randint(0, 8, (10,)); metrics = calculate_regime_metrics(pred, labels); print('Metrics calculated successfully')" executes without errors</verify>
  <done>Comprehensive validation metrics and monitoring system implemented</done>
</task>

<task type="auto">
  <name>Task 4: Model checkpointing and state management</name>
  <files>src/training/trainer.py, src/models/model_utils.py</files>
  <action>Implement model checkpointing system in RegimeTrainer and create model_utils.py. Add checkpoint saving for best validation accuracy and latest state with metadata including epoch, metrics, optimizer state, scheduler state, and training configuration. Implement checkpoint loading with validation of architecture compatibility and configuration consistency. Add model state utilities: save_model_state, load_model_state, and verify_model_compatibility functions. Include automatic checkpoint cleanup keeping only best and recent checkpoints. Add model export functionality for inference-only deployment. Implement checkpoint recovery for interrupted training sessions with full state restoration.</action>
  <verify>python -c "from src.training.trainer import RegimeTrainer; from src.models.model_utils import save_model_state, load_model_state; from src.models.regime_detector import RegimeDetector; model = RegimeDetector(); trainer = RegimeTrainer(model); trainer.save_checkpoint('test_checkpoint.pth', epoch=1, metrics={'val_acc': 0.8}); print('Checkpoint saved successfully')" executes without errors</verify>
  <done>Model checkpointing and state management system implemented with full recovery capabilities</done>
</task>

<task type="auto">
  <name>Task 5: Training script and integration</name>
  <files>scripts/train_regime_detector.py, src/training/trainer.py</files>
  <action>Create executable training script in scripts/ directory that brings together all components. Implement argument parsing for training parameters, data configuration, and output paths. Add data loading using TrainingDataCollector with proper error handling and validation. Implement full training loop with RegimeTrainer including progress monitoring, metric logging, and checkpoint management. Add training resumption capability from existing checkpoints. Include final model evaluation on test set with comprehensive metrics report. Add visualization of training progress with loss curves and validation metrics. Implement proper logging setup with file output and console display. Include training summary report generation.</action>
  <verify>python scripts/train_regime_detector.py --help shows usage information and python -c "from scripts.train_regime_detector import main; print('Script imported successfully')" works without errors</verify>
  <done>Complete training script implemented with full integration of all training components</done>
</task>

<task type="auto">
  <name>Task 6: Comprehensive test suite for training system</name>
  <files>tests/training/test_trainer.py, tests/training/test_metrics.py, tests/training/__init__.py</files>
  <action>Create comprehensive test suite for training infrastructure. Test RegimeTrainer initialization, training step mechanics, validation step, loss calculation with different configurations. Test checkpoint saving/loading with state verification and model compatibility checks. Test metrics calculation for various input scenarios including edge cases. Test early stopping logic with different patience settings and improvement thresholds. Test device management and mixed precision training functionality. Include integration tests with small synthetic datasets to verify full training pipeline without long runtime. Test training resumption and state recovery mechanisms. Add performance tests for training efficiency and memory usage.</action>
  <verify>pytest tests/training/ -v passes all tests and python -c "from src.training.trainer import RegimeTrainer; from src.data.training_data import create_data_loaders; trainer = RegimeTrainer(); train_dl, val_dl, test_dl = create_data_loaders(years=1); trainer.train_epoch(train_dl); print('Training epoch test completed')" executes successfully</verify>
  <done>Comprehensive test suite implemented covering all training functionality with integration testing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Training configuration properly integrated into existing config system
- [ ] RegimeTrainer handles both classification and confidence regression with combined loss
- [ ] Validation metrics include per-regime accuracy, confidence calibration, and temporal stability
- [ ] Model checkpointing saves best and latest models with full state recovery
- [ ] Training script integrates all components with proper error handling
- [ ] Early stopping prevents overfitting with configurable patience
- [ ] Tests validate training mechanics and integration with sample data
- [ ] Device management works for both CPU and GPU environments
- [ ] Mixed precision training implemented for efficiency
- [ ] Training resumption works correctly from saved checkpoints
</verification>

<success_criteria>

- All tasks completed successfully with comprehensive implementation
- RegimeTrainer implements complete training pipeline with monitoring
- Model validation includes regime classification metrics, confidence calibration analysis, and temporal stability
- Checkpointing system preserves best performing models and enables training recovery
- Training script provides end-to-end training capability with visualization
- Test suite validates all training components with integration testing
- **Phase 2 complete: Regime detection system fully trained and validated**
- Ready for Phase 3: Strategy Framework implementation

</success_criteria>

<output>
After completion, create `.planning/phases/02-regime-detection/02-03-SUMMARY.md`:

# Phase 2 Plan 3: Training Loop & Validation Summary

**Completed comprehensive training infrastructure for regime detection with validation and checkpointing**

## Accomplishments

- Complete training loop with combined classification + confidence loss function
- Comprehensive validation metrics including regime accuracy and confidence calibration
- Model checkpointing system with state management and recovery capabilities
- Training script providing end-to-end training with monitoring and visualization
- Mixed precision training for efficiency with device management
- Early stopping and learning rate scheduling for optimal training

## Files Created/Modified

- `src/config.py` - Added TrainingConfig with comprehensive hyperparameters
- `src/training/__init__.py` - Training module initialization
- `src/training/trainer.py` - Complete RegimeTrainer implementation
- `src/training/metrics.py` - Validation metrics and monitoring utilities
- `src/models/model_utils.py` - Model checkpointing and state management
- `scripts/train_regime_detector.py` - Executable training script
- `tests/training/test_trainer.py` - Training infrastructure test coverage
- `tests/training/test_metrics.py` - Metrics calculation test coverage
- `tests/training/__init__.py` - Training test module

## Decisions Made

- **Combined Loss Function**: 80% classification (CrossEntropyLoss) + 20% confidence (MSELoss) for balanced training
- **Learning Rate Schedule**: ReduceLROnPlateau with factor 0.5, patience 5 for adaptive learning
- **Early Stopping**: Patience 10 epochs with minimum improvement delta 1e-4
- **Checkpointing Strategy**: Save best validation accuracy model + latest state for recovery
- **Validation Approach**: Temporal holdout preserving chronological order for realistic evaluation
- **Device Management**: Automatic CUDA detection with fallback to CPU
- **Mixed Precision**: Enabled for efficiency while maintaining numerical stability

## Issues Encountered

[Problems and resolutions during implementation, or "None"]

## Phase Status

✅ **Phase 2: Regime Detection COMPLETE**

Ready for Phase 3: Strategy Framework - Implementation of 16 core options strategies with standardized interface.
</output>