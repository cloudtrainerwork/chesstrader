---
phase: 05-strategy-selector
plan: 01
type: execute
---

<objective>
Implement strategy selector neural network that ranks and recommends options strategies based on market state and regime.

Purpose: Build the core decision-making system that evaluates all 16 strategies using the spatial neural architecture to provide ranked recommendations with confidence scores.
Output: Working strategy selector network that integrates with SpatialNet to evaluate and rank strategies for given market conditions.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase dependencies from previous work:
@.planning/phases/03-strategy-framework/03-04-SUMMARY.md
@.planning/phases/04-neural-architecture/04-03-SUMMARY.md

# Key files from existing implementation:
@src/models/spatial_net.py
@src/models/spatial_encoder.py
@src/models/regime_detector.py
@src/strategies/factory.py

**Tech stack available:** PyTorch, scikit-learn, yfinance, sqlalchemy
**Established patterns:** Neural network architecture with spatial encoding, strategy framework with 16 types, regime-based classification
**Constraining decisions:**
- Phase 3: Complete 16-strategy framework with StrategyFactory
- Phase 4: SpatialNet with chess-inspired architecture and transfer learning
- Project: Strategy selection for automated decision-making

**Key integration:** Connect spatial neural architecture with strategy evaluation to rank strategies.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement strategy ranking neural network</name>
  <files>src/models/strategy_selector.py, tests/models/test_strategy_selector.py</files>
  <action>Create StrategySelector class that uses SpatialNet features to rank strategies. Implement forward() method that takes market state and outputs strategy scores for all 16 strategies. Add strategy_ranking_head that connects to SpatialNet's feature extraction layers. Include softmax scoring for probability distribution over strategies. Add top_k_strategies() method returning ranked list with confidence scores. Implement strategy_compatibility_check() to validate market conditions for each strategy. Use existing SpatialNet from Phase 4 as feature extractor. Output shape should be (batch_size, 16) for all strategy types.</action>
  <verify>pytest tests/models/test_strategy_selector.py passes, all 16 strategies get scored, rankings are properly normalized</verify>
  <done>Strategy selector network implemented, rankings produced for all strategies, confidence scores properly calibrated</done>
</task>

<task type="auto">
  <name>Task 2: Integration with SpatialNet and regime detection</name>
  <files>src/models/strategy_selector.py, src/models/integrated_selector.py, tests/models/test_integrated_selector.py</files>
  <action>Create IntegratedStrategySelector that combines StrategySelector with RegimeDetector outputs. Implement regime_weighted_ranking() that adjusts strategy scores based on regime probabilities. Add market_state_encoding() that combines regime features with spatial features. Include confidence_thresholding() to filter out low-confidence recommendations. Implement get_recommendation() that returns top strategy with rationale. Add batch prediction support for multiple market states. Integrate with existing StrategyFactory for baseline comparison. Ensure compatibility with all 8 regime types from Phase 2.</action>
  <verify>pytest tests/models/test_integrated_selector.py passes, regime weighting works correctly, recommendations align with market conditions</verify>
  <done>Integrated selector combines regime and spatial features, recommendations properly weighted by regime confidence, batch processing works</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/models/test_strategy_selector.py` passes
- [ ] `pytest tests/models/test_integrated_selector.py` passes
- [ ] All 16 strategies receive scores for any market state
- [ ] Rankings are normalized and interpretable as probabilities
- [ ] Regime integration improves recommendation quality
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Strategy selector ranks all 16 strategies based on market conditions
- Regime detection properly integrated into ranking process
- Confidence scores provide meaningful recommendation strength
- System ready for scoring logic implementation in 05-02
  </success_criteria>

<output>
After completion, create `.planning/phases/05-strategy-selector/05-01-SUMMARY.md`:

# Phase 5 Plan 1: Strategy Selector Network Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key selector network outcome 1]
- [Key integration outcome 2]

## Files Created/Modified

- `src/models/strategy_selector.py` - Description
- `src/models/integrated_selector.py` - Description
- `tests/models/test_*.py` - Description

## Decisions Made

[Key strategy selection decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 05-02-PLAN.md: Scoring logic and recommendation engine implementation
</output>