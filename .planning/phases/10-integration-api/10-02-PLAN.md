---
phase: 10-integration-api
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: [
  "src/cli/__init__.py",
  "src/cli/main.py",
  "src/cli/commands/__init__.py",
  "src/cli/commands/recommend.py",
  "src/cli/commands/backtest.py",
  "tests/cli/test_cli_main.py",
  "tests/cli/test_commands.py"
]
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can run 'chesstrader recommend AAPL' from command line"
    - "User can run 'chesstrader backtest --symbol AAPL' from command line"
    - "CLI provides formatted output with tables and colors"
  artifacts:
    - path: "src/cli/main.py"
      provides: "Typer CLI application entry point"
      min_lines: 40
      exports: ["app"]
    - path: "src/cli/commands/recommend.py"
      provides: "Strategy recommendation command"
      contains: "@app.command()"
    - path: "src/cli/commands/backtest.py"
      provides: "Backtesting command"
      contains: "@app.command()"
  key_links:
    - from: "src/cli/main.py"
      to: "src/main.py"
      via: "OptionsAI import and instantiation"
      pattern: "from.*main import.*OptionsAI"
    - from: "src/cli/commands/*.py"
      to: "rich"
      via: "Table and console output formatting"
      pattern: "from rich.*Table|Console"
---

<objective>
Create modern CLI interface using Typer that provides user-friendly access to strategy recommendations and backtesting.

Purpose: Deliver professional command-line experience with rich formatting and intuitive commands
Output: Complete CLI application with recommend and backtest commands
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-integration-api/10-RESEARCH.md
@.planning/phases/10-integration-api/10-01-SUMMARY.md

# Interface from previous plan
@src/main.py
</context>

<interfaces>
<!-- Key interfaces from Plan 01 that this plan uses -->

From src/main.py:
```python
class OptionsAI:
    def __init__(self, config_path: Optional[str] = None):

    async def get_recommendations(self, symbol: str, **kwargs) -> List[Dict[str, Any]]:
        """Get strategy recommendations for symbol"""

    async def run_backtest(self, config: Dict[str, Any]) -> Dict[str, Any]:
        """Run complete backtest workflow"""

    def get_strategy_details(self, strategy_name: str) -> Dict[str, Any]:
        """Get detailed strategy information"""
```

From src/config.py:
```python
class Config:
    def __init__(self, config_path: Optional[str] = None):
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create CLI application structure</name>
  <files>src/cli/__init__.py, src/cli/main.py, src/cli/commands/__init__.py</files>
  <action>
    Set up Typer CLI application structure:

    **src/cli/__init__.py:**
    - Empty init file for package

    **src/cli/main.py:**
    - Import Typer and create main app instance
    - Import OptionsAI from src.main
    - Create global OptionsAI instance with error handling
    - Add shared CLI configuration (help text, version info)
    - Set up Rich console for formatted output
    - Add global options for config path and verbosity

    **src/cli/commands/__init__.py:**
    - Empty init file for commands package

    Follow Typer application structure from research, preparing for command modules.
  </action>
  <verify>python -c "from src.cli.main import app; print(app.info.name)" shows app info</verify>
  <done>CLI application structure created and importable</done>
</task>

<task type="auto">
  <name>Task 2: Implement recommend command</name>
  <files>src/cli/commands/recommend.py</files>
  <action>
    Create strategy recommendation command with rich formatting:

    - Import Typer, Rich Table, and OptionsAI
    - Define recommend command with symbol argument
    - Add options: --confidence (default 0.4), --max-results (default 3), --details (flag for extended info)
    - Call ai.get_recommendations() with proper async handling
    - Format output using Rich Table with columns: Strategy, Score, Confidence, Details (if requested)
    - Handle errors gracefully with user-friendly messages
    - Add help text explaining each parameter
    - Include color coding: high confidence (green), medium (yellow), low (red)

    Follow CLI command pattern from research with Rich integration for professional output.
  </action>
  <verify>python -c "from src.cli.commands.recommend import recommend; print(recommend.__name__)" imports successfully</verify>
  <done>Recommend command created with rich table formatting</done>
</task>

<task type="auto">
  <name>Task 3: Implement backtest command and wire CLI</name>
  <files>src/cli/commands/backtest.py, tests/cli/test_cli_main.py, tests/cli/test_commands.py</files>
  <action>
    **Backtest command (src/cli/commands/backtest.py):**
    - Define backtest command with options: --symbol, --start-date, --end-date, --strategy (optional)
    - Build config dict from CLI arguments
    - Call ai.run_backtest() with async handling
    - Format results using Rich console with summary metrics
    - Show key results: total return, Sharpe ratio, max drawdown, win rate
    - Add progress indication for long-running backtests
    - Handle errors and provide meaningful feedback

    **Register commands in src/cli/main.py:**
    - Import and add recommend and backtest commands to main app
    - Configure command help and descriptions

    **Tests (tests/cli/):**
    - test_cli_main.py: Test app initialization, global options
    - test_commands.py: Test command parsing and execution with mocks
    - Use Typer testing utilities for CLI testing
    - Mock OptionsAI methods to avoid external dependencies

    Wire all components following Typer patterns from research.
  </action>
  <verify>
    <automated>python -m pytest tests/cli/ -v</automated>
  </verify>
  <done>Complete CLI with recommend and backtest commands, fully tested</done>
</task>

</tasks>

<verification>
- CLI commands execute without import errors
- Recommend command produces formatted table output
- Backtest command handles configuration and displays results
- Rich formatting enhances user experience
- All CLI tests pass with proper mocking
</verification>

<success_criteria>
- Professional CLI interface using Typer and Rich
- Strategy recommendations displayed in readable table format
- Backtesting accessible through intuitive command-line interface
- Error handling provides helpful user feedback
- Comprehensive test coverage validates CLI functionality
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-api/10-02-SUMMARY.md`
</output>