---
phase: 04-neural-architecture
plan: 01
type: execute
---

<objective>
Implement spatial encoder that converts options positions and market data into chess-inspired spatial representations.

Purpose: Create the foundation for game-theoretic AI modeling by mapping options strategy positions to spatial game board representations, enabling transfer learning from chess AI architectures.
Output: Working spatial encoder that transforms position models into 7x6 spatial tensors with integrated market regime context.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase dependencies from frontmatter analysis:
@.planning/phases/02-regime-detection/02-01-SUMMARY.md
@.planning/phases/01-data-pipeline/01-03-SUMMARY.md

# Key files from existing implementation:
@src/models/regime_detector.py
@src/features/position_models.py
@src/features/position_state.py

**Tech stack available:** PyTorch, scikit-learn, yfinance, sqlalchemy
**Established patterns:** Neural network architecture with batch normalization, position zone mapping, regime-based classification
**Constraining decisions:**
- Phase 2: 8-regime classification with confidence scoring
- Phase 1: 24-dimensional position vectors and 7-zone spatial mapping
- Phase 3: 16 strategy types with standardized interface

**Key innovation:** Treating options strategies as bounded games where strike prices act as board boundaries, price movements represent piece positions, enabling chess AI transfer learning.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Position-to-spatial mapping implementation</name>
  <files>src/models/spatial_encoder.py, tests/models/test_spatial_encoder.py</files>
  <action>Create SpatialEncoder class that maps options positions to 7x6 spatial tensors. Implement position_to_spatial() method that takes Position objects and converts them to chess-board-like representations where: rows represent price levels (7 zones from PositionZones), columns represent option legs/strikes (up to 6), cell values encode option type, quantity, and Greeks. Include validate_spatial_dimensions() to ensure 7x6 output shape. Use existing PositionZones enum for spatial mapping. Avoid hardcoding magic numbers - use constants for board dimensions.</action>
  <verify>pytest tests/models/test_spatial_encoder.py passes, spatial tensors have shape (7, 6) for all strategy types</verify>
  <done>Position objects correctly converted to 7x6 spatial tensors, all 16 strategy types supported, spatial dimensions validated</done>
</task>

<task type="auto">
  <name>Task 2: Market state encoding integration</name>
  <files>src/models/spatial_encoder.py, src/models/market_encoder.py, tests/models/test_market_encoder.py</files>
  <action>Create MarketEncoder class that integrates regime detection outputs with position spatial data. Implement encode_market_state() method that combines RegimeDetector 8-regime probabilities + confidence with spatial position tensors. Add market context as additional channels to spatial representation (position channel + regime channel + confidence channel = 3-channel spatial tensor). Include regime confidence thresholding for decision quality. Use existing RegimeDetector from Phase 2. Ensure tensor concatenation preserves spatial dimensions (7x6xC format for CNN processing).</action>
  <verify>pytest tests/models/test_market_encoder.py passes, market state correctly integrated with spatial positions, output tensors have shape (C, 7, 6) where C>=3</verify>
  <done>Market regime information successfully encoded into spatial representation, regime confidence properly integrated, multi-channel spatial tensors ready for CNN processing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/models/test_spatial_encoder.py` passes
- [ ] `pytest tests/models/test_market_encoder.py` passes
- [ ] All 16 strategy types produce valid 7x6 spatial tensors
- [ ] Regime detection integration works with existing RegimeDetector
- [ ] No errors or warnings in spatial tensor generation
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Spatial encoder converts positions to 7x6 game board representations
- Market regime context integrated into spatial tensors
- Multi-channel tensor format ready for convolutional processing
- Full test coverage for spatial encoding functionality
  </success_criteria>

<output>
After completion, create `.planning/phases/04-neural-architecture/04-01-SUMMARY.md`:

# Phase 4 Plan 1: Spatial Encoder Implementation Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key spatial encoding outcome 1]
- [Key market integration outcome 2]

## Files Created/Modified

- `src/models/spatial_encoder.py` - Description
- `src/models/market_encoder.py` - Description
- `tests/models/test_spatial_encoder.py` - Description

## Decisions Made

[Key spatial representation decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 04-02-PLAN.md: Residual blocks and attention mechanism implementation
</output>